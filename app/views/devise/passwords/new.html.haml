%h2 忘记您的密码？
= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f|
  = devise_error_messages!
  %div
    = f.label "电子邮件"
    %br/
    = f.email_field :email, :autofocus => true
  %div= f.submit "给我发送密码重置确认信息"
= render "devise/shared/links"
